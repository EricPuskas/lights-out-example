/**
 * Defines the grid size props interface
 */
export interface GridSizeProps {
  active: boolean;
}

/**
 * Defines the grid size item interface
 */
export interface GridSizeItem {
  size: number;
  label: string;
}
