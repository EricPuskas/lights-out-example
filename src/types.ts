/**
 * Defines the NeonColor type
 */
export type NeonColor = "blue" | "orange";

/**
 * Defines the Cell interface
 */
export interface Cell {
  active: boolean;
  positionX: number;
  positionY: number;
}
